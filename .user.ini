; PHP settings for Azure App Service (read by PHP-FPM from the web root, applies to every folder below it).
; Session cookie hardening; the site is served over HTTPS only.
session.cookie_httponly = 1
session.cookie_secure = 1
session.cookie_samesite = "Lax"
session.use_strict_mode = 1
session.use_only_cookies = 1